// Defines the possible match result codes stored in the CSV.
// Exported so the same definition can be shared between different files.
//
// enum = enumeration
export enum MatchResult
{
    HomeWin = "H",
    AwayWin = "A",
    Draw = "D"
};